# **Input:** Household id, start date of the graph, and end date of the graph (MM/DD/YY)
# **Output:** A horizontal bar graph using the character `@` with the max energy generation as twenty `@`'s for each day within the date range (round down)
def main(id: int, start_date: str, end_date: str) -> str:
    # YOUR CODE HERE
    return
